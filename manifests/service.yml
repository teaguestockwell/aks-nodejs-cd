apiVersion: v1
kind: Service
metadata:
    name: "buildableaks1-ebcc"
    labels:
        app: "buildableaks1-ebcc"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "buildableaks1-ebcc"